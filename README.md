## Configure the database

1. Start a local MySQL server.
2. Copy/paste [`.env.example`](.env.example) at the root of the project, naming the new file `.env` and filling the required data.

## Build and run

Install dependencies

```text
npm install
```

Build

```text
npm run build
```

Run

```text
npm run start
```
